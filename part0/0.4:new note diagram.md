
```mermaid
sequenceDiagram
    Browser->>Server: HTTP Post request to new_note
    Server-->>Browser: HTTP 302
    Note right of Browser: This is a URL redirect; asking the browser to make a HTTP Get request to the adress in header "notes"
    Browser->>Server: main.js
    Browser->>Server: main.css
    Browser->>Server: data.json

```
